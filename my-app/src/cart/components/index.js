import React, { Component } from 'react';
import Bar from '../../common/components/bar/bar';

const Cart = ({barlist, completed, text}) => (
	<div className="cart-box">
		<div>敬请期待！</div>
		{/*底部导航*/}
		{/*<Bar barlist={barlist}></Bar>*/}
	</div>
)

export default Cart